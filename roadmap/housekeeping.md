# Housekeeping

## Work Unit Summary

**Status:** active

**Problem/Intent:**
Clean up build artifacts and project structure to prevent staleness issues and improve developer experience.

---

## TODO

- [ ] **Move CSS input file**: Relocate `static/css/input.css` to a more appropriate location (e.g., `styles/input.css` or root `input.css`). The `static/` directory should only contain generated/output files.
- [ ] **Remove generated files from git**: Several generated files are tracked despite gitignore rules (added before rules existed). Need to:
  - `git rm --cached static/css/output.css`
  - `git rm --cached templates/*_templ.go`
  - `git rm --cached internal/db/*.go`
  - Update Makefile if CSS input path changes

---

## Notes

The gitignore already has the correct rules:
```
/internal/db/
templates/*_templ.go
static/css/output.css
```

These files are generated by:
- `make css` → `static/css/output.css`
- `make templ` → `templates/*_templ.go`
- `make sqlc` → `internal/db/*.go`

They should be built at deploy time (Dockerfile) and during local dev (`make generate`, `make css`).
